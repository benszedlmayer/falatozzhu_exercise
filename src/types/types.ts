export type Item = {
  id: number;
  name: string;
  number: number;
  description: string;
  image: string;
};

export type FormInputs = {
  name: string;
  number: string;
  description: string;
};
